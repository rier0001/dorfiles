[module/date]
type                = internal/date
interval            = 1

date                = %a %d %b | %H:%M

label               = %date%
label-foreground    = ${colors.foreground}
label-background    = ${colors.transparent}
